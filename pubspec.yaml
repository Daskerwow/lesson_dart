name: lesson_dart
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none # ← add this

environment:
  sdk: ^3.13.0

dependencies:
  meta: ^1.19.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
